schema_version: "0.4"
name: "graph-chain-reaction"
command: "python -m arena.cli run --scenario graph-chain-reaction"
workdir: ../..
fixture_policy: by_hash
strict: true
budget_thresholds:
  max_tool_calls: 6
  max_tokens: 500
contracts:
  config: ../../contracts/graph-chain-reaction.contracts.yaml

schema_version: "0.4"
name: "secret-karaoke"
command: "python -m arena.cli run --scenario secret-karaoke"
workdir: ../..
fixture_policy: by_hash
strict: true
budget_thresholds:
  max_tool_calls: 4
  max_tokens: 500
contracts:
  config: ../../contracts/secret-karaoke.contracts.yaml


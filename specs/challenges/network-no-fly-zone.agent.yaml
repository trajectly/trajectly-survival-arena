schema_version: "0.4"
name: "network-no-fly-zone"
command: "python -m arena.cli run --scenario network-no-fly-zone"
workdir: ../..
fixture_policy: by_hash
strict: true
budget_thresholds:
  max_tool_calls: 3
  max_tokens: 500
contracts:
  config: ../../contracts/network-no-fly-zone.contracts.yaml

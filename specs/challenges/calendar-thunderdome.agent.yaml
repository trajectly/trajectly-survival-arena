schema_version: "0.4"
name: "calendar-thunderdome"
command: "python -m arena.cli run --scenario calendar-thunderdome"
workdir: ../..
fixture_policy: by_hash
strict: true
budget_thresholds:
  max_tool_calls: 6
  max_tokens: 500
contracts:
  config: ../../contracts/calendar-thunderdome.contracts.yaml


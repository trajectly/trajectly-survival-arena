version: v1
tools:
  allow:
    - fetch_logs
    - post_summary
sequence:
  require:
    - tool:fetch_logs
    - tool:post_summary
data_leak:
  outbound_kinds:
    - TOOL_CALL
  secret_patterns:
    - "sk_live_[A-Za-z0-9_]+"

